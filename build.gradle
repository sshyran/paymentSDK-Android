apply plugin: 'maven'

group='com.github.wirecard'
version='release'

artifacts {
  archives new File('paymentsdk-release.aar')
}

uploadArchives {
  repositories {
    mavenDeployer {
      repository(url: mavenLocal().url)
    }
  }
}
