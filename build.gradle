apply plugin: 'maven'

group='com.github.mposSVK'
version='release'

artifacts {
  archives new File('paymentsdk-release.aar')
}

uploadArchives {
  repositories {
    mavenDeployer {
      repository(url: mavenLocal().url)
    }
  }
}
